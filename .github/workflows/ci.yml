{
  "name": "CI",
  "on": {
    "push": {
      "branches": [
        "master"
      ]
    }
  },
  "jobs": {
    "build": {
      "runs-on": "ubuntu-latest",
      "steps": [
        {
          "name": "Checkout",
          "uses": "actions/checkout@v2.4.0"
        },
        {
          "name": "Nix",
          "uses": "cachix/install-nix-action@v16",
          "with": {
            "extra_nix_config": "access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}"
          }
        },
        {
          "name": "Cachix",
          "uses": "cachix/cachix-action@v10",
          "with": {
            "name": "dendrite-demo-pinecone",
            "signingKey": "'${{ secrets.CACHIX_SIGNING_KEY }}'"
          }
        },
        {
          "name": "Build",
          "run": "nix build"
        },
        {
          "name": "Build docker image",
          "run": "nix build .#dockerImage"
        },
        {
          "name": "Load docker image",
          "run": "docker load < ./result"
        },
        {
          "name": "Tag docker image",
          "run": "docker tag dendrite-demo-pinecone ghcr.io/bbigras/dendrite-demo-pinecone"
        },
        {
          "name": "Log in to ghcr.io",
          "run": "echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u bbigras --password-stdin"
        },
        {
          "name": "Push to ghcr.io",
          "run": "docker push ghcr.io/bbigras/dendrite-demo-pinecone"
        }
      ]
    }
  }
}
