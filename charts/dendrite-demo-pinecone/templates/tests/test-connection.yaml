apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dendrite-demo-pinecone.fullname" . }}-test-connection"
  labels:
    {{- include "dendrite-demo-pinecone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dendrite-demo-pinecone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
